# 版本更新记录

## 版本历程总览

| 版本 | 日期 | 核心变更 |
|------|------|---------|
| v0.1 | 初始 | 自定义 RTOS 内核完成（调度器 + IPC + 内存管理）|
| v0.2 | — | Bootloader 完成（裸机跳转，无更新逻辑）|
| v0.3 | — | Bootloader + App 联合调试，修复跳转 HardFault |
| v0.4 | — | 引入 FOTA 框架，完成 PC 上位机 |
| v0.5 | 2026-07-16 | 全链路 FOTA 联调通过 |
| v0.6 | 2026-07-16 | 内存分配器从 first-fit 升级为 heap4 算法 |
| v0.7 | 2026-07-16 | 就绪位图 O(n)→O(1) + CLZ 指令优化调度 |
| v0.8 | 2026-07-16 | 全量中英文注释 + 编码 UTF-8 统一 + PendSV ASCII 栈图 |
| v0.9 | 2026-07-16 | Flash 互斥锁 + 持锁阻塞检测 + TaskCreateEX 可配置栈大小 |

---

## v0.1 — 初始 RTOS 内核

**变更内容：**
- 实现 PendSV 汇编级上下文切换
- 实现 16 级抢占式调度器
- 实现二值信号量、互斥锁（优先级继承 PIP）、消息队列
- 实现 Best-fit 动态内存分配器
- 实现异步日志系统 LOGI → PrintQueue → DMA

**设计选择：**
- 为什么不用 FreeRTOS？学习目的，完全掌控内核行为
- 为什么用 PendSV 做上下文切换？Cortex-M3 专为 RTOS 设计的异常，延迟最低
- 为什么内存用 best-fit？当时对分配器性能没有深入考虑，准备后续演进

---

## v0.2 — Bootloader 初始版本

**变更内容：**
- CubeMX 生成空白工程，仅保留 GPIO + RCC 外设
- 读取 0x08004000 处栈指针 → 校验 → 跳转

---

## v0.3 — Bootloader + App 联调，修复跳转 HardFault

**问题：** 烧录后运行到 `char *msg = "... RTOS System Starting ..."` 处立即 HardFault

**排查过程：**
1. 对比两个项目的 HAL 时基配置：Bootloader 用 SysTick，App 用 TIM1
2. 分析跳转链路：Bootloader 的 `HAL_Init()` 启动了 SysTick → 跳转前只调用了 `__disable_irq()` → 但 `HAL_RCC_OscConfig()` 内部调用了 `__enable_irq()` 无条件开启了 PRIMASK
3. 积压的 SysTick 中断触发 → App 的 `SysTick_Handler` 访问了 NULL 的 `runninglist` → 空指针 BusFault

**修复：** Bootloader 跳转前完整清理外设状态

**思考：** 嵌入式系统跳转的难点不在于"跳过去"，而在于"把当前环境清理干净让新程序能正常启动"。SysTick、NVIC、外设时钟的残留状态都会导致不可预测的 bug。

---

## v0.4 — FOTA 框架 + 标志位页隔离设计

**变更内容：**
- flash_update.c/h：FOTA 任务、CRC16、帧解析器、Flash 驱动
- PC_Tool：Python 上位机
- Bootloader 增加 `copy_new_firmware()`

**关键设计决策：更新标志位页隔离**

**问题：** 最初标志位放在 Bootloader 最后一字 (0x08001FFC)，与 Bootloader 共享第 7 页

**分析：** Flash 写前必须先擦除整页 (1KB)。如果标志和第 7 页的 Bootloader 代码共存，写标志时会擦掉 Bootloader

**方案对比：**

| 方案 | 优点 | 缺点 |
|------|------|------|
| 标志位放 Bootloader 末尾 | 不浪费空间 | 共享页，擦标志 = 擦 Bootloader |
| 标志位独占第 7 页 | 完全隔离，安全 | 需要压缩 Bootloader |
| 标志位放 RTC 备份寄存器 | 不占 Flash | 依赖电池/电容，掉电丢失 |

**最终选择：** Bootloader 从 16KB 压缩到 7KB，第 7 页完全独立为标志页

**为什么选这个：** 零依赖、零风险、零浪费。压缩 Bootloader 后还释放了 1KB（原 16KB → 7KB，但布局是 7+1+28+28）

---

## v0.4.1 — 擦除期间 IWDG 超时复位

**问题：** FOTA 擦除 Staging 区时系统约 8 秒无响应，按按键后立即复位

**排查过程：**
1. 计算 IWDG 超时：`Prescaler=4, Reload=3000` → LSI 40kHz → 300ms 超时
2. 每擦一页 Flash 锁总线 ~30ms，CPU 无法取指令
3. SysTick 被延迟 → IWDG 不能及时刷新
4. 28 页 × 30ms = 840ms > 300ms → IWDG 复位

**思考：** 这是一个典型的"中断饿死"问题。硬件看门狗的刷新依赖于 SysTick 中断的正常运行，而 Flash 擦除锁总线直接阻止了 SysTick 取指。解决方案不能是"别擦太久"——而是每步操作后显式刷新 IWDG + 让出 CPU。

**修复：** 每页擦除后立即 `HAL_IWDG_Refresh()` + `taskdelay(10)`

---

## v0.4.2 — 栈溢出 HardFault

**问题：** IWDG 修复后按键仍然复位，但延迟变长

**排查过程（静态分析，未用调试器）：**
1. 查 `TASK_DEFAULT_STACK_SIZE = 256` 字 = 1024 字节
2. `FlashUpdateTask_Entry` 中有 `staging_buf[2048]`，**2KB 局部变量在 1KB 栈上**
3. 加上 `Frame_t`(~260B) → 远超栈容量
4. 栈溢出冲毁相邻内存 → 按键任务读取被破坏的 TCB → HardFault

**修复：** 2KB 缓冲区改为 `static`

**教训：** RTOS 任务栈默认 1KB，大数组必须放 .bss 段。代码审查时"找局部数组大小"是检查栈溢出最快的方法。

---

## v0.4.3 — UART 发送冲突

**问题：** HAL_UART_Transmit 永远返回 HAL_BUSY，PKT_READY 发不出

**根因：** PrintTask 使用 DMA 持续发送 LOG，HAL 的 `gState` 永远为 `HAL_UART_STATE_BUSY_TX`。FOTA 任务的任何 HAL TX 调用都会被拒绝。

**思考：** 这暴露了一个架构问题——两个发送路径（PrintTask DMA + FOTA 直接写入）相互不知道对方的存在。它们在 HAL 层面没有任何协调机制。

**修复方案对比：**

| 方案 | 分析 | 结论 |
|------|------|------|
| 轮询 HAL gState | 仍然需要协调 | 不可靠 |
| 关中断直接写寄存器 | 绕过 HAL 状态机，但关中断太长 | 临时方案 |
| **走 PrintQueue 统一通道** | 所有 TX 通过同一个 DMA 通道 | ✅ 最终方案 |

**最终方案：** 改造 `LogMsg_t` 增加 `len` 字段，`send_packet` 将二进制帧打包成 `LogMsg_t` 通过 `PrintQueue` 发送，`PrintTask` 用 `rxMsg.len` 替代 `strlen`。

---

## v0.4.4 — UART RX 中断未启动

**问题：** PKT_READY 能发出，但 PC 发送数据后 MCU 不回复 ACK

**根因：** `HAL_UART_Receive_IT(&huart2, &g_rx_byte, 1)` **从未被调用过**。HAL 的 UART RX 状态机在首次触发前不会进入接收状态。

**思考：** 这是 HAL 库的一个惯用陷阱——使能 NVIC 中断不等于启动了 RX 接收链。必须显式调用 `HAL_UART_Receive_IT` 来初始化内部状态机。

---

## v0.5 — 全链路 FOTA 联调通过

**关键 Bug 修复：Bootloader 0xFFFFFFFF 断点**

**问题：** 升级成功，MCU 复位后直接 HardFault。CRC32 校验正常、PKT_OK 已收到

**排查过程：**
1. 确认 Staging 区数据完整（CRC32 校验通过才写标志位）
2. 查看 Bootloader `copy_new_firmware` 第 91 行：

```c
word = *(__IO uint32_t*)src;
if (word == 0xFFFFFFFF) break;  // "数据到此为止"
```

3. `.bin` 文件中天然存在 0xFFFFFFFF 填充（BSS 段、对齐间隙）
4. Bootloader 读到第一个 0xFF 就停止拷贝，后面几 KB 实际代码没拷过去

**同时修复** FOTA `write_flash_buffer` 中 `if (word != 0xFFFFFFFF)` 跳过逻辑。

**思考：** 0xFFFFFFFF 在 Flash 中是"已擦除"状态，但在二进制固件中是**合法数据值**。任何以 0xFF 作结尾标记的逻辑在固件升级场景下都是 bug。这个 bug 潜伏了很久——以前固件小，0xFF 出现在末尾；加了 Flash HAL 驱动后固件变大，0xFF 提前出现触发了 bug。

---

## v0.5.1 — 传输期间中断锁死

**问题：** PC 传输固件时按键无响应，传输结束后按键操作一次性爆发

**根因：** `write_flash_buffer` 写入 2KB = 512 个 word，`HAL_FLASH_Program` 内 `__disable_irq` 每次约 45µs，累积锁中断 ~23ms。EXTI 按键中断被物理屏蔽。

**修复：** 每写 128 字让步一次（`taskdelay(3)`），接收循环每 80 字节让步一次。

---

## v0.6 — 内存分配器升级：first-fit → heap4 best-fit

### 问题背景

当前 `my_os_malloc` 使用了 first-fit 策略（变量名虽为 `best_block`，但循环中遇到第一个满足条件的块就 `break`）。虽然当前场景下无实际缺陷，但随着系统功能增多（FOTA 任务、更多 IPC 对象），碎片风险会逐步累积。

### 算法选型分析

在决定升级方向前，系统评估了三种主流动分配方案：

#### 方案 1：位图内存池 (Bitmap Memory Pool)

**原理：** 将内存划分为固定大小的块，用位图标记每个块的占用状态。

**优点：**
- 分配/释放 O(1)，性能极高
- 实现简单（几十行代码）
- 无外部碎片

**缺点：**
- **强依赖于 MMU/VMP —— 这是硬伤。** 位图池只能分配固定大小块，不同大小请求需要不同池，要求虚拟内存系统提供连续地址映射。没有 MMU 的单片机上，要为每个对象尺寸建一个池，要么浪费大量内存要么限制灵活性
- 内部碎片严重——最坏情况浪费近 50%
- 任务栈需要自定义尺寸，很难匹配固定池

**结论：** ❌ 不适用于 STM32F103（无 MMU，20KB SRAM 太小）

#### 方案 2：伙伴算法 (Buddy Allocator)

**原理：** 将内存按 2 的幂分割成伙伴块，合并时只合并同级的伙伴。

**优点：**
- 分配速度 O(log N)
- 合并简单（只需检查伙伴的标识位）
- 外部碎片较少

**缺点：**
- **内部碎片严重**——最坏情况浪费近 50%。例如请求 1300 字节 → 分配到 2048 字节块
- 元数据开销大——需要跟踪每个块的层级和伙伴状态
- 对于 20KB SRAM，2 的幂分割导致大量空间浪费（12KB 堆 → 最多 4K+4K+2K+1K+... 的分割组合）
- 实现复杂，约 200 行

**结论：** ❌ 在 12KB 堆上性价比太低，内部碎片不可接受

#### 方案 3：heap4 算法 (Best-fit + 边界标记 + 最小碎片约束)

**原理：** FreeRTOS heap4 的核心设计：
1. 空闲块按地址排序成单链表
2. 分配时遍历全部块，选尺寸最小的满足块（best-fit）
3. 释放时按地址插入，前后合并
4. 最小块尺寸约束：`heapMINIMUM_BLOCK_SIZE`，防止产生无法复用的超小碎片
5. 边界标记（可选）：在块头和块尾都存 size，释放时无需遍历即可找到相邻块

**优点：**
- 外部碎片最少（best-fit + 前后合并在堆分配器中是最优组合）
- 最小块约束确保每个空闲块都能被后续分配利用
- 实现简单（~120 行），容易验证正确性
- 额外内存开销仅 4-8 字节/块（链表指针 + size）

**缺点：**
- 分配 O(N)，随碎片数增加线性变慢。对于 12KB 堆，最坏情况几十个碎片，几十微秒可忽略
- 长时间运行仍可能碎片化，但速度远慢于 first-fit

**我的选择：** ✅ **heap4（改造成双向链表）**

**理由：**
1. 12KB 堆 + 典型使用模式（任务固定创建、IPC 持久），碎片风险低但存在
2. Best-fit 对碎片控制优于 first-fit，且开销可以忽略
3. 最小块约束防止了当前 first-fit 可能产生的死碎片
4. 双向链表使 malloc 中移除块的操作 O(1)，释放中插入更简洁
5. **学习价值高**——heap4 是嵌入式领域最广泛使用的堆分配算法之一，理解它对后续 RTOS 移植/选型很有帮助

### 具体改动

| 文件 | 行 | 改动 |
|------|-----|------|
| `Core/Src/rtos.h` | — | 新增 `#define HEAP_MIN_BLOCK_SIZE` |
| `Core/Src/rtos.c` | 33-37 | `MemBlock_t` 增加 `prev` 指针，变为双向链表 |
| `Core/Src/rtos.c` | 48-102 | `my_os_malloc`：first-fit → best-fit，增加最小块检查 |
| `Core/Src/rtos.c` | 105-148 | `my_os_free`：适配双向链表插入，合并逻辑不变 |

### 数据结构对比

```c
// 改前 (v0.5)
typedef struct MemBlock {
    struct MemBlock *next;
    uint32_t size;
} MemBlock_t;

// 改后 (v0.6)
#define HEAP_MIN_BLOCK_SIZE  (sizeof(MemBlock_t) + 8)

typedef struct MemBlock {
    struct MemBlock *next;
    struct MemBlock *prev;     // ← 新增
    uint32_t size;
} MemBlock_t;
```

### 分配策略变化

```c
// 改前：first-fit（碰到第一个能用的就停）
while (curr != NULL) {
    if (curr->size >= total_size) {
        best_block = curr;  // 第一匹配
        break;              // ← 停！
    }
    curr = curr->next;
}

// 改后：best-fit（遍历全部，挑最小的）
while (curr != NULL) {
    if (curr->size >= total_size && curr->size < best_size) {
        best = curr;
        best_size = curr->size;  // ← 持续更新
    }
    curr = curr->next;
}
```

### 验证

1. 编译通过，0 error 0 warning
2. RTOS 正常启动，6 个业务任务 + 1 FOTA 任务创建成功
3. FOTA 升级全流程正常
4. `my_os_malloc`/`my_os_free` 在 IdleTask 任务删除回收路径上正常

---

## v0.7 — 就绪位图：O(n)→O(1) 优先级查找

### 动机

原始 TaskSwitch 用 for 循环扫描 16 级优先级寻找最高优先级的就绪任务：



最坏情况循环 16 次。虽然规模不大，但调度器是系统最频繁调用的路径（每次 SysTick + 每次任务切换），理论上应该以 O(1) 为目标。

### 算法选型

位图 (Bitmap) + CLZ (Count Leading Zeros) 是 Cortex-M 上实现 O(1) 调度的标准方案——FreeRTOS、RT-Thread 都用同样的方法。

| 方案 | 时间复杂度 | 代码量 | 可行性 |
|------|-----------|--------|--------|
| for 循环（原方案） | O(N) N=16 | 5 行 | ✅ 当前在用，但不够优雅 |
| **位图 + CLZ** | **O(1)** | **4 行** | **✅ 单周期指令，Native 支持** |
| 硬件查找表 | O(1) | 大 | ❌ 浪费 64KB Flash 做查表不值 |

### 实现方案

引入 16 位无符号整数 ，Bit N = 1 表示 readyList[N] 中有就绪任务。配合 Cortex-M3 的  指令（Count Leading Zeros，单周期），一次计算即可得出最高优先级。



### 具体改动

全部在  和  中：

| 位置 | 改动 |
|------|------|
| rtos.c 全局变量 | 新增  |
| rtos.h | 新增  |
| taskMoveInReady (行 194) | 插入就绪链表时： |
| taskMoveOutList (行 225) | 链表变空时： |
| TaskSwitch (行 378-379) | 删除 for 循环，替换为 CLZ 查位图 |
| TaskDelete (行 682) | RUNNING 任务自删除的位图保护 |

### 关键边界处理：RUNNING 任务自删除

 对 RUNNING 状态的任务直接 return（不执行删除逻辑）。如果 RUNNING 任务调用  删除自己，位图得不到清零 → 后续调度器以为该优先级还有任务 → 操作 NULL 指针 → HardFault。

在  中增加了后置保护：



### 收益

- TaskSwitch 中优先级查找从 16 次循环 → 1 条 CLZ 指令（单周期）
- 代码更简洁，去掉了 for 循环 + break 的嵌套
- 与主流 RTOS（FreeRTOS、RT-Thread）的调度实现对齐

### 风险与限制

| 风险 | 评估 | 缓解 |
|------|------|------|
|  兼容性 | ARMCC V5 原生支持  内嵌函数 | 已验证，无需额外头文件 |
| 编译器优化 |  在任何优化级别都映射到 CLZ 指令 | 单周期，不受影响 |
| 位图与链表不一致 | 如果修改就绪链表但忘了改位图 | 极小概率，代码审查可见 |

---

## v0.8 — 全量注释 + 编码统一

### 背景

跨工具链编码冲突：Keil 使用 GB2312（Windows ANSI）保存 .c 文件，而我使用的 Python 脚本默认用 UTF-8 读写。 导致 67% 的中文字节被静默丢弃，生成的乱码无法恢复。

### 解决方案

1. 放弃恢复原始已损坏注释，直接为所有 API 函数编写**全新的中文 + 英文双语注释**
2. 删除残留的乱码行，去除重复注释块
3. 在 PendSV_Handler 和 TaskCreate 处添加 ASCII 字符画栈帧布局图
4. 用户需要在 Keil 中设置 **Edit → Configuration → Encoding → UTF-8** 以匹配文件编码

### 改动范围

| 文件 | 注释量 | 说明 |
|------|--------|------|
| Core/Src/rtos.c | ~200 行 | 调度器/IPC/内存管理/汇编全量注释 + 栈图 |
| Core/Src/main.c | ~50 行 | 全部任务入口 + LOGI + 中断回调 |
| Core/Src/rtos.h | ~10 行 | 枚举定义英文标注 |
| My_bootloader/Core/Src/main.c | ~10 行 | 跳转逻辑 + 搬运函数 |

### 教训

Python 操作含中文的源文件必须确保编码一致。以后所有 .c/.h 文件统一使用 UTF-8，Keil 也要设置为 UTF-8。CubeMX 新生成的文件如果携带中文，需要先转码再纳入工程。

---

## v0.9 — Flash 互斥锁 + 持锁检测 + TaskCreateEX

### 背景

在 FOTA 升级与业务任务并存的系统中，Flash 控制器的并发访问缺乏软件层保护。虽然 HAL 库硬件层面不会让两个擦写同时进行（BSY 标志位），但高优先级任务抢占低优先级的 Flash 操作任务后，会导致高优先级任务自旋等待，浪费 CPU 甚至引发死锁。

同时，任务栈大小固定为 256 字（1KB），无法针对不同任务的需求定制。FOTA 任务的 `Frame_t`(260B) + 缓冲区需要大栈，而 IdleTask 和 PwmTask 的栈需求极小——统一分配浪费了宝贵的 SRAM。

### 变更 1：Flash 操作加互斥锁

所有 Flash 擦写函数（`erase_staging_area`、`write_flash_buffer`、`erase_flag_page`、`set_update_flag`）在 `HAL_FLASH_Unlock` 前获取 `MutexTake(FlashMutex)`，在 `HAL_FLASH_Lock` 后释放 `MutexGive(FlashMutex)`。利用现有 Mutex 的优先级继承协议（PIP），在发生多任务竞争时自动避免优先级反转。

### 变更 2：持锁期间阻塞检测

在 TCB 中新增 `uint8_t held_mutex_count` 字段，每次 MutexTake/MutexGive 时增减。在 `taskdelay`、`SemaphoreTake`、`QueueSend`、`QueueReceive` 四个阻塞 API 入口调用 `check_no_mutex_held()`，若检测到当前任务持有互斥锁则直接操作 USART2 寄存器输出崩溃信息并死循环等待 IWDG 复位。

**设计理念**：不从"出了死锁怎么解"的角度处理，而从"为什么不一开始就防止"的角度——阻塞 API 持锁就是一个 bug。

### 变更 3：可配置任务栈大小

| 改动 | 文件 |
|------|------|
| `TaskCreate` → `TaskCreateEX` 增加 `stack_words` 参数 | Core/Src/rtos.c |
| 新增 `TaskCreate` 包装函数，默认栈 128 字 | Core/Src/rtos.c |
| `TASK_DEFAULT_STACK_SIZE` 256→128 | Core/Src/rtos.h |
| IdleTask: 256→64 字 | Core/Src/rtos.c |
| PwmTask: 256→64 字 | Core/Src/main.c |
| FlashUpd: 256→200 字 | Core/Src/main.c |

**内存收益**：8 任务从 8KB 栈占用降至约 3.9KB，节省 4.1KB SRAM（约 50%）。

### 思考过程：双核适用性

在 ESP32 双核架构下，RUNNING 任务剥离就绪链表的设计天然避免了同任务在两核上并发——FreeRTOS SMP 需要额外临界区保护。虽然当前项目只跑在 STM32F103 单核上，但这个设计在架构层面是更安全的。

### Bug 修复：SemaphoreTake 误增 held_mutex_count

**问题：** 系统启动后立即进入 `check_no_mutex_held` 的死循环。log `HAL_UART_Transmit`、`SemaphoreTake` 等普通操作都会触发。

**排查：** 在 `check_no_mutex_held` 条件后加 `&& 0` 临时禁用检测，发现系统恢复正常——说明有任务在不持有锁的情况下 `held_mutex_count > 0`。

**定位：** Python 批量补丁脚本的模式匹配误伤了 `SemaphoreTake`——其内部 `if (sem->count == 1)` 分支结构恰好与 `MutexTake` 的持锁判断类似，导致被插入了两行 `held_mutex_count++`。信号量 Take 与互斥锁无关，不应修改该字段。

**修复：** 从 `SemaphoreTake` 中删除错误插入的 `held_mutex_count++`。现确认 `held_mutex_count` 只在 `MutexTake`（两条路径）和 `MutexGive` 中出现。

**教训：** 批量代码生成必须基于语义匹配（函数签名），不能依赖结构相似性（如 `if (xxx->count == 1)`）。补丁脚本需要在替换后进行语义级别的交叉验证。
