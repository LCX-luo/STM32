# ST7789 SPI 显示屏适配报告

## 问题现象

1. 屏幕只亮背光，无任何显示内容
2. 反复插拔杜邦线，偶有一次能正常显示
3. 不接 DC 和 RES 线时，屏幕出现规律性雪花闪烁
4. 万用表测量 DC 引脚有电压跳变（说明 GPIO 在输出）
5. 烧录不包含 LCD 驱动的旧固件，背光同样亮但无显示

## 排查过程

### 第一步：排除硬件接线

| 测试 | 方法 | 结论 |
|------|------|------|
| 通断测试 | 万用表蜂鸣档测每根线 | 所有线导通 |
| RES 测试 | GPIO 循环拉低/拉高 RES，观察屏闪 | 屏规律性闪烁，RES 通路正常 |
| SPI 测试 | 发送 SLPOUT+DISPON 命令后全屏写红像素 | 代码执行完成，但屏幕无红色 |

### 第二步：定位根因

以上测试确认了：
- **GPIO 和 SPI 硬件通路正常**
- **MCU 代码完整执行**
- **数据发了但屏幕没收到（或没理解）**

ST7789 驱动 IC 支持两种 SPI 模式：**Mode 0**（CPOL=Low, CPHA=1Edge）和 **Mode 3**（CPOL=High, CPHA=2Edge）。

项目中初始配置为 **Mode 0**，偶发能显示说明这个模式在时序边缘，依赖于上电瞬间的时钟相位差恰好对齐。

## 根本原因：SPI 模式不匹配

```
SPI Mode 0 (默认配置):
  SCLK 空闲 = Low
  数据在上升沿采样
  时钟空闲期无跳变

SPI Mode 3 (ST7789 最常用模式):
  SCLK 空闲 = High  
  数据在上升沿采样
  但 SCK 空闲时保持 High，第一个边沿是下降沿
```

部分 ST7789 模块的**内部采样逻辑更倾向于 Mode 3**。Mode 0 虽然理论上也能工作，但**时钟极性和相位差会导致第一个字节的起始位被误采样**，表现为：

- 初始化命令没被识别 → 屏一直保持在复位/睡眠状态
- 偶发正常 = 上电瞬间 SCK 的毛刺恰好让采样对齐了

## 为什么"偶尔插拔能正常"？

这是 SPI 模式不匹配的典型现象：

```
拔插 → 屏掉电 → 电容放电 → 重新上电
         → SCK 引脚浮空时可能有毛刺
         → 毛刺恰好触发了正确的时钟边沿
         → 第一个命令被正确接收 → 后面都同步了

但大多数时候毛刺不对 → 第一个字节错位 → 后面串行全错
```

## 修复

在 CubeMX 中修改 SPI1 配置：

| 参数 | 改前 (Mode 0) | 改后 (Mode 3) |
|------|--------------|--------------|
| CPOL | Low | **High** |
| CPHA | 1 Edge | **2 Edge** |

重新生成代码后，显示正常。

## 经验教训

1. **SPI 模式必须查数据手册确认**。ST7789 手册明确列出支持 Mode 0 和 Mode 3，但实际模块的 PCB 走线和信号质量可能导致一种模式更可靠。
2. **万用表测 DC 电压跳变只能说明 GPIO 在输出，不能说明 SPI 数据被屏正确接收。** 逻辑分析仪才能看到真实的 SPI 时序。
3. **"拔插偶尔正常"是 SPI 模式不匹配的特有征兆**——如果完全不通（接线错误），拔插一百万次也不会正常。
4. 对于新手调试 SPI 屏，**先试 Mode 3**（CPOL=High, CPHA=2Edge）成功率最高。ST7735/ST7789/ILI9341 等常见 IC 都优先支持 Mode 3。

---

# Bug 2：CPU 占用率计算错误 + 显示残留

## 问题现象

1. 系统监控面板显示 `CPU Idle: 117%`（不可能超过 100%）
2. Uptime 显示 `Uptime: 9s` 变为 `Uptime: 10s` 时，末尾残留 `s` 和之前的字符
3. CPU 占用率一直显示 80%，开关呼吸灯不变

## 排查过程

### CPU 计算错误

第一版算法用 `idle_count++` 在 IdleTask 主循环中递增，每 500ms 采样计算：

```c
cpu_idle = (idle - prev_idle) * 100 / dt;
```

问题：IdleTask 循环极快（每次 ~1μs），500ms 内运行几十万次，远大于 `dt=500`。所以结果永远远超 100%。

### 第二版改用 sw_wdg_counter

用 `sw_wdg_counter`（每 1ms SysTick 自增，IdleTask 清零）来计算：

```c
sw_delta = sw_val - prev_sw;
cpu_idle = 100 - (sw_delta * 100 / dt);
```

问题：IdleTask 在 SysMon 读数之前已经把 counter 清零了，`sw_delta` 永远为 0 → `cpu_idle` 永远 100%。

### 最终方案：idle_tick_count

在 **SysTick_Handler** 中判断当前任务：

```c
if (runninglist && runninglist->taskTCB.priority == 0)
    idle_tick_count++;
```

每 1ms Systick，如果当前任务是 IdleTask（优先级 0），就计 1ms 空闲。500ms 周期内累计值直接就是空闲毫秒数，除以总时间就是精确空闲率。

### 显示残留

`snprintf("Uptime: %lus", now / 1000)` 输出变长字符串。`"Uptime: 9s"` 到 `"Uptime: 10s"` 时，`9s` 后面的空白没被覆盖。

修复：固定宽度 `"%4lus"`，输出始终占 4 个字符位置。

### CPU 占用率不变

呼吸灯（PwmTask）开/关对 CPU 占用率影响极小，因为 PwmTask 在 `taskdelay` 中阻塞，不消耗 CPU。SysMon 自身 1s 刷新一次的 SPI 传输消耗反而更大。

## 修复

1. **CPU 计算**：添加 `volatile uint32_t idle_tick_count`，在 SysTick 中递增。SysMon 二次采样的差值 / 时间间隔 = 真实空闲率
2. **显示残留**：所有 snprintf 数值段固定宽度（`%4lu`、`%3lu`）
3. **测试工具**：按键每次切换 HogTask 创建/删除，HogTask 是纯 CPU 计算（无阻塞），通过观察 CPU Idle 变化验证算法正确

## 涉及文件

| 文件 | 改动 |
|------|------|
| Core/Src/rtos.c | 新增 idle_tick_count 变量 + SysTick 中判断 IdleTask 计数 + 声明导出 |
| Core/Src/rtos.h | extern idle_tick_count + sw_wdg_counter |
| Core/Src/main.c | ledtask 双版本（FOTA_DEMO_NEW/PB5 只控制 vs 正常呼吸+PB5）；
               SysMon 改用 idle_tick_count 算 CPU + 固定宽度显示 + FOTA 模式标题 |
| Core/Src/main.c | 新增 CpuHogTask_Entry 负载测试函数，按键创建/删除 |

## 经验教训

1. **CPU 占用率只能用中断计数，不能在任务循环中计数。** IdleTask 的循环速度和 CPU 性能/编译优化相关，不是稳定的时间基准。
2. **IdleTask 清零 counter 的时机比 SysMon 读数早**——采样法和清零法不能共存。
3. **显示文字必须固定宽度。** 嵌入式屏没有自动清屏，变长字符串一定残留。（或者每次全屏刷新，但性能代价太大）

---

# Bug 3：按键响应状态不一致——HogTask 切换逃逸引脚二次确认

## 问题现象

快速短按按键时，HogTask 创建/删除与灯光切换出现状态不一致：串口 Log 显示 HogTask 已创建/删除，但呼吸灯/PB5 未翻转；或者灯光变了但 HogTask 状态没变。两种行为「脱节」。

## 排查过程

### 第一步：审查 ledtask 执行流程

发现现有代码结构存在 **判定条件分裂**：

```c
void ledtask(void *arg)
{
    while (1)
    {
        SemaphoreTake(ButtonSem);   // 等待按键中断
        taskdelay(20);               // 软件消抖

        // 【路径 A】无条件执行：HogTask 切换
        if (g_hog_task == NULL)
            g_hog_task = TaskCreate(...);
        else
        {   TaskDelete(g_hog_task);
            g_hog_task = NULL;    }

        // 【路径 B】有条件执行：灯光翻转
        if (HAL_GPIO_ReadPin(GPIOA, GPIO_PIN_10) == GPIO_PIN_SET)
        {
            breathing_mode = !breathing_mode;
            // PB5 翻转...
        }
    }
}
```

**路径 A 无保护，路径 B 有保护**。当按键按下的时间 < 20ms（消抖延时）时，消抖完引脚已回落为低电平，路径 B 不执行，但路径 A 已执行。一次轻触导致「HogTask 变了，灯没变」。

### 第二步：分析二值信号量的局限

`SemaphoreGive` 在中断中一直只唤醒 `waitList` 头部第一个任务。如果放开 `GenericButtonTask_Entry`（它也在等 `ButtonSem`），两个任务竞争 signal，一次按键只有一个任务得到唤醒，另一任务完全收不到事件。

但当前 main.c 中 `GenericButtonTask_Entry` 被注释了，所以只有一个消费任务。问题收敛为：**同任务内两个操作的保护条件不一致**。

### 第三步：深挖时序不一致

进一步分析发现还有两层隐患：

1. **信号量 count 0/1 导致快速连按丢事件**：第一次按键 Give 将 count 设为 1，ledtask 的 Take 消耗后 count=0。若在 `taskdelay(20)` 消抖期间第二次按键发生，中断 Give 再次设置 count=1，但 ledtask 还阻塞在消抖中，没有 Take。消抖结束后 Take 走第二个事件——但若在 Take → 消抖窗口内第三次按键发生，count 已是 1（Give 不会累加），第三次事件丢失。

2. **TaskDelete 异步回收内存**：`TaskDelete` 仅将任务加入 `tasksWaitingTermination`，由 IdleTask 后续回收。若快速连续按键，HogTask 删除后紧接着再次创建，IdleTask 尚未回收完毕，`my_os_malloc` 可能因内存不足返回 NULL，导致 HogTask 创建静默失败。

## 修复

### 修复 1：统一判定条件——所有操作放入引脚确认内部

将 HogTask 切换移入 `if (HAL_GPIO_ReadPin(...))` 内部，与灯光翻转共享同一保护条件：

```c
if (HAL_GPIO_ReadPin(GPIOA, GPIO_PIN_10) == GPIO_PIN_SET)
{
    // HogTask 切换和灯光切换在同一保护下——原子执行
    breathing_mode = !breathing_mode;
    if (breathing_mode) {
        // 呼吸模式：PB5 亮 + 创建 HogTask
        if (g_hog_task == NULL)
            g_hog_task = TaskCreate(...);
    } else {
        // 常亮模式：PB5 灭 + 删除 HogTask
        if (g_hog_task != NULL) {
            TaskDelete(g_hog_task);
            g_hog_task = NULL;
        }
    }
    // ...等松开
}
```

**核心原则：所有响应动作必须在一处判定，不分裂。**

### 修复 2：HogTask 优先级 0 防止看门狗饿死

原设计 `CpuHogTask_Entry` 优先级 2，死循环永不阻塞，导致 IdleTask（优先级 0）无法运行 → `sw_wdg_counter` 不归零 → 9 秒后软件看门狗复位。

修复：HogTask 改为优先级 0（与 IdleTask 同级），两者时间片轮转。IdleTask 有 CPU 时间喂狗。

### 修复 3：TaskDelete 后 taskdelay(5) 等待 IdleTask 回收

在 `TaskDelete` 后立即调用 `taskdelay(5)`，给 IdleTask 一个时间片执行回收操作。确保连续按键时 HogTask 内存及时释放，下次 `TaskCreate` 不因 OOM 失败。

## 涉及文件

| 文件 | 改动 |
|------|------|
| Core/Src/main.c | ledtask 重构，HogTask 切换与灯光绑定；HogTask 优先级 0 |

## 经验教训

1. **判定条件的统一性是状态一致性的前提。** 两个操作不能一个被保护一个不被保护——要么都受保护，要么都不受。
2. **二值信号量的「一次性」不是 bug，是特性。** 理解这个特性才能正确设计 ISR → 任务的通信模型。需要一个消费任务接收一个事件时，信号量是最优解。需要广播时，应该用事件标志或队列。
3. **死循环任务必须和其他任务共享 CPU。** 最低优先级也不能独占——要配合 `taskdelay` 或 IdleTask 的时间片机制。
4. **异步回收必须考虑回收延迟。** `TaskDelete` 后紧接着 `TaskCreate` 可能因内存未回收而失败，必须在两次操作间插入让步点。

---

# Bug 4：CPU 占用率不随 HogTask 变化——优先级判断误伤

## 问题现象

HogTask 创建后，CPU Load 显示完全不变（始终 ~2%），但堆内存正常增减（任务确实被创建/删除了）。

## 排查过程

### 第一步：检查 idle_tick_count 逻辑

SysTick 中判断代码：

```c
if (runninglist && runninglist->taskTCB.priority == 0)
    idle_tick_count++;
```

HogTask 的优先级在 Bug 3 修复中被设为 0（为了与 IdleTask 同优先级以让出 CPU 喂狗）。

**问题：HogTask 优先级 0，IdleTask 也是 0。 runninglist->priority == 0 把 HogTask 也算成了「空闲」**。

```
时间轴 (每 1ms SysTick)：
  ms 1  IdleTask → idle_tick_count++ ✅
  ms 2  HogTask  → idle_tick_count++ ❌（应该是忙）
  ms 3  IdleTask → idle_tick_count++ ✅
  ms 4  HogTask  → idle_tick_count++ ❌
  500ms 后 idle_tick_count ≈ 250（填了 500 中的 250）
  cpu_usage = 100 - 250*100/500 = 50% → 但显示 ~2%
```

推导发现理论应该是 50%，但显示 ~2%——这意味着 `idle_tick_count` 几乎等于 500（填满了整个采样周期）。说明 **HogTask 创建后几乎没怎么跑，优先级 0 的两个任务中 IdleTask 仍然占据绝大多数 CPU**。

进一步排查发现死循环：

```c
void CpuHogTask_Entry(void *arg)
{
    volatile uint32_t x = 0;
    while (1) {
        for (int i = 0; i < 100000; i++)
            x += i * i;
    }
}
```

`for` 循环 100000 次 ≈ 约几十 ms。但 `for` 循环内没有 `taskdelay()`、没有阻塞——while(1) 一次迭代直接进入下一次 `for`。Cortex-M3 同优先级时间片轮转，应给 IdleTask 一半时间。`volatile` 关键词禁止了编译器优化，每次读写 x 都走内存，导致该循环实际远慢于预期——几乎等价于一个纯粹的 **内存读写延迟器**，反而让 IdleTask 有机会在时间片间隙插进来。

但即便这样，CPU 也应该是 ~50%——显示 ~2% 说明 HogTask 可能根本没拿到等量的时间片。

### 第二步：修正判断条件

不应按优先级判定，应按 **任务指针** 判定：

```c
// 全局保存 IdleTask 的 TCB 指针
TaskList *idle_task_ptr = NULL;
// 创建时赋值
idle_task_ptr = TaskCreateEX(IdleTask_Entry, NULL,0, 64, (unsigned char *)"OS_Idle");

// SysTick 中判定
if (runninglist == idle_task_ptr)   // 只有真正的 IdleTask
    idle_tick_count++;
```

这样 HogTask（即使优先级也是 0）不会被计入空闲计数。

## 修复

1. 新增全局变量 `TaskList *idle_task_ptr`，在 `StartScheduler` 中赋值
2. SysTick 判定 `runninglist == idle_task_ptr` 替代 `priority == 0`
3. CPU 数据恢复到正确值

## 涉及文件

| 文件 | 改动 |
|------|------|
| Core/Src/rtos.c | 新增 idle_task_ptr 变量 + SysTick 判定改用指针比对 |
| Core/Src/rtos.h | 导出 idle_task_ptr |

## 经验教训

**优先级 != 任务身份。** 不要用优先级来判定「是不是空闲任务」——同优先级的任务可能有完全不同的性质。绑定到具体的 TCB 指针是唯一可靠的方式。
