# STM32 自定义 RTOS + FOTA 固件在线升级 项目报告

## 一、项目概述

本项目基于 **STM32F103C8T6** (ARM Cortex-M3, 64KB Flash, 20KB SRAM)，从零构建了一套完整的嵌入式软件系统，分为三大组件：

| 组件 | 功能 | 关键文件 |
|------|------|---------|
| **自定义 RTOS** | 手写抢占式实时操作系统：PendSV 汇编调度、信号量、互斥锁(PIP)、消息队列 | `Core/Src/rtos.c` |
| **My_bootloader** | 自定义引导加载器：更新标志检测、Flash 页搬运、芯片外设清理 | `My_bootloader/Core/Src/main.c` |
| **FOTA 在线升级** | MCU 端 Flash 更新任务 + PC 端 Python 上位机 | `Core/Src/flash_update.c`, `PC_Tool/` |

**技术栈：** ARMCC V5.06, STM32CubeMX HAL, Python 3 + tkinter + PySerial

---

## 二、系统架构设计

### 2.1 Flash 内存布局 (64KB 零浪费)

```
地址范围                  页号     大小     用途
0x08000000 ─ 0x08001BFF    0-6      7KB     Bootloader 代码
0x08001C00 ─ 0x08001FFF     7       1KB     更新标志位（独立页）
0x08002000 ─ 0x08008FFF   8-35     28KB     App 应用程序
0x08009000 ─ 0x0800FFFF  36-63     28KB     Staging Area（新固件暂存）
```

**设计决策：**
- App 和 Staging **完全等大**（28KB），确保任意版本的固件都能完整暂存
- 标志位独占一个 Flash 页（第 7 页），擦写时**零风险**触碰 Bootloader 代码
- Bootloader 从 16KB 压缩到 7KB（实际代码仅 ~2.5KB），释放出更多空间给新固件

### 2.2 Bootloader 决策树

```
上电复位
  → HAL_Init() → SystemClock_Config() → MX_GPIO_Init()
  → 读取 *(uint32_t*)0x08001FFC
    ├─ == 0xA5A5A5A5 (MAGIC_UPDATE_READY)
    │    → copy_new_firmware()
    │       1. 擦除 App 区 (28 页)
    │       2. 从 Staging 逐字拷贝 28KB
    │       3. 擦除 Staging 区 (28 页)
    │       4. 擦除标志位页 (回到 0xFFFFFFFF)
    │    → 跳转到 App
    └─ == 0xFFFFFFFF (无更新)
         → 直接跳转到 App
```

### 2.3 FOTA 完整时序

```
PC 上位机 (Python)                        STM32 MCU                             Bootloader
    │                                         │                                     │
    │                                         ├─ FlashUpdateTask 上电启动            │
    │                                         ├─ erase_staging_area() (28页)        │
    │                                         ├─ send_packet(PKT_READY)             │
    │                                         ├─ 循环 taskdelay(5) 等待...          │
    │  (用户随时连上串口)                       │                                     │
    │                                         ├─ PKT_READY 每 3 秒重发一次           │
    │                                         │                                     │
    │←──── PKT_READY ────────────────────────│                                     │
    │                                         │                                     │
    │─PKT_INFO(size, CRC32)────────────────→│                                     │
    │←ACK───────────────────────────────────│                                     │
    │                                         │                                     │
    │─PKT_DATA(seq=0, 256B+CRC16)──────────→│                                     │
    │                                         ├─ CRC16 校验 → ACK                    │
    │                                         ├─ 累积 2KB → write_flash_buffer()    │
    │←ACK───────────────────────────────────│                                     │
    │  ... (循环 N 轮)                         │                                     │
    │                                         │                                     │
    │─PKT_COMPLETE──────────────────────────→│ 写 MAGIC 到 0x08001FFC               │
    │←PKT_OK────────────────────────────────│                                     │
    │                                         ├─ NVIC_SystemReset()                 │
    │                                         │        ↓ 复位                        │
    │                                         │        ┌────────────────────        │
    │                                         │        │ 检查 0x08001FFC              │
    │                                         │        │ = 0xA5A5A5A5 → 搬运         │
    │                                         │        │ → 跳转 App                   │
    │                                         │        └────────────────────        │
    │  App 重新启动                             │                                     │
```

### 2.4 PC 上位机架构

- **语言:** Python 3 + tkinter (标准库自带) + PySerial
- **界面:** 串口终端 + 升级控制面板 一体化
- **特性:** 毫秒级时间戳、异常字符过滤、超时重传（3次）、进度条

---

## 三、RTOS 核心特性

| 特性 | 说明 |
|------|------|
| **调度方式** | 抢占式优先级调度，16 级 (0-15)，PendSV 异常实现上下文切换 |
| **上下文切换** | ARM 汇编保存/恢复 R4-R11，首次运行特殊处理 PSP 零值 |
| **任务状态** | READY → RUNNING → BLOCKED → SUSPEND → DELETE 完整五态流转 |
| **IPC 机制** | 二值信号量（按优先级排序等待）、互斥锁（优先级继承协议 PIP）、消息队列（环形缓冲 + 阻塞读/写）|
| **内存管理** | Best-fit 动态分配器，12KB 独立堆，支持相邻空闲块合并 |
| **看门狗** | 软件 WDT（9 秒，IdleTask 喂狗）+ 硬件 IWDG（300ms）|
| **日志系统** | LOGI() → 消息队列 → PrintTask → DMA UART 发送，业务任务零阻塞 |
| **特殊约束** | `TaskDelete(NULL)` 不删除自身；信号量 ISR 不能调用（会意外 `__enable_irq`）；无软件定时器 |

---

## 四、Bug 排查与修复实录（按时间线）

> 以下是本项目在开发过程中遇到的所有关键问题，以及系统化的排查-分析-修复过程。

### Bug #1: 修改 IROM 地址后读取值未变化

**现象：** 在 Keil 魔术棒 Target → IROM1 Start=0x8004000 后，用工具查看 uvprojx 中仍显示 `IROM(0x8000000-0x800FFFF)`。

**排查：** 逐行比对 uvprojx XML 结构，发现 `<Cpu>` 标签中的 IROM 是器件数据库的**物理芯片描述**（只读），用户设置实际存储在 `<OCR_RVCT4>` 标签内。两个标签的值可以不同，这是 Keil 的正常设计。

**结论：** 区分"器件级描述"和"链接器级配置"——修改已经生效，只是看错字段了。

---

### Bug #2: Bootloader → App 跳转后 HardFault

**现象：** 烧录 Bootloader + App 后，运行到 `char *msg = "--- RTOS System Starting ---"` 处立即 HardFault，地址在 `0x0800844A`（ADR 指令）。

**排查过程：**

1. **对比两个项目的 HAL 时基配置：** Bootloader 使用默认 SysTick，App 使用 TIM1
2. **分析跳转链路：** Bootloader 的 `HAL_Init()` 启动了 SysTick → 跳转前只调用 `__disable_irq()` 屏蔽中断，但 **SysTick 硬件仍在计数** → App 的 `SystemClock_Config()` → `HAL_RCC_OscConfig()` 内部调用 `__enable_irq()` 无条件开启了 PRIMASK → 积压的 SysTick 中断立即触发
3. **关键：** App 的 `SysTick_Handler`（rtos.c）读取了 `runninglist->taskTCB`，但此时 `runninglist` 还是 NULL（RTOS 未初始化）→ 空指针访问 → BusFault → HardFault

**修复：** Bootloader 跳转前增加外设清理序列：

```c
SysTick->CTRL = 0;                // 停止 SysTick 计数器
SCB->VTOR = APP_START_ADDRESS;    // 设置向量表偏移
for (int i = 0; i < 8; i++) {
    NVIC->ICER[i] = 0xFFFFFFFF;   // 清除所有 IRQ 使能
    NVIC->ICPR[i] = 0xFFFFFFFF;   // 清除所有挂起 IRQ
}
__HAL_RCC_APB1_FORCE_RESET();     // 复位外设总线
```

**教训：** Bootloader 跳转 App 不仅仅是设置 MSP 和跳转 PC——必须把 MCU 恢复到接近上电复位的状态。否则外设残留状态会在新程序初始化过程中触发意外中断。

---

### Bug #3: 更新标志位页的隔离设计

**初始方案 (废弃):** 标志位放在 Bootloader 区域最后一字 `0x08001FFC`，与 Bootloader 共享第 7 页。

**风险分析：** Flash 写入前必须先擦除整页 (1KB)。标志位写入时如果擦除第 7 页，该页上无论 1 字节还是 1023 字节都是 Bootloader 代码——会一起被清掉。

**最终方案:** Bootloader 缩小到 7KB (页 0-6)，第 7 页完全独立为"标志位专用页"。擦除这一页时**绝对不触碰** Bootloader 的任何代码。

**设计原则：** Flash 操作的最小单位是页 (1KB)，任何需要独立擦写的区域必须独占至少一个完整页。

---

### Bug #4: FOTA 擦除期间系统卡死、按键触发看门狗复位

**现象：**

1. 擦除期间串口 8 秒无任何输出（之前每隔 500ms 都有 LOG）
2. 擦除完成后按一下按键 → 系统立即复位重启

**分析过程：**

1. **查 IWDG 时钟树：** `Prescaler=4, Reload=3000` → LSI 40kHz ÷ 4 = 10kHz → 3000 个 tick = **300ms 超时**
2. **查 Flash 擦除时序：** 每擦一页约 **30ms** 且期间 Flash 总线锁死，CPU 无法从 Flash 取指令
3. **查 SysTick_Handler 代码：** `HAL_IWDG_Refresh()` 的调用在 SysTick ISR 里。擦除时 SysTick ISR 被延迟执行 → IWDG 累积超时
4. **算累计时间：** 原始代码一次擦 28 页不间断 → 28 × 30ms = 840ms > 300ms → **硬件看门狗触发复位**

**修复：** 每页擦除后立即：

```c
for (每页) {
    HAL_FLASHEx_Erase(...);       // ~30ms
    HAL_IWDG_Refresh(&hiwdg);     // 铁打不动的看门狗刷新
    taskdelay(10);                 // 让出 CPU 给 PrintTask 发日志
    LOGI("FOTA: Erasing... %d/28");
}
```

**验证：** 修复后擦除日志持续输出（每 ~44ms 一条），按键正常切换呼吸灯，无复位。

---

### Bug #5: 栈溢出触发 HardFault

**现象：** Bug #4 修复后，擦除完成，按按键过几秒仍会复位。

**静态分析（未使用调试器）：**

1. 查 RTOS 配置：`TASK_DEFAULT_STACK_SIZE = 256`（字），即 **1KB 栈/任务**
2. 查 `FlashUpdateTask_Entry` 的局部变量：
   - `staging_buf[2048]` → **2KB**
   - `Frame_t frame` → ~260 字节
   - 其他变量 → ~20 字节
   - **总计约 2.3KB，远超 1KB 栈**
3. 栈从高地址向低地址增长，溢出后冲毁相邻内存（可能是其他任务的 TCB）
4. 按按键 → 按键任务读取 TCB → 碰已损坏数据 → BusFault → IWDG 300ms 后复位

**修复：** 2KB 缓冲区从栈上改为 `.bss` 段（static）：

```c
// 当前任务栈上: Frame_t(~260B) + 其他(~20B) = ~280B < 1KB ✅
static uint8_t s_staging_buf[2048];   // 放 .bss 段
static uint8_t s_tx_buf[271];         // 放 .bss 段
```

**教训：** 默认栈大小（1KB）对于任何堆缓冲区都是极限数字。大缓冲区必须用 `static` 或动态分配。

---

### Bug #6: UART 发送冲突导致 PKT_READY 发不出去

**现象：** PC 上位机打开串口后收到 MCU 的 LOG 输出（带时间戳），但**永远收不到 PKT_READY 帧**，串口终端显示有持续数据流但没有 `0xAA 0x01` 开头的数据。

**分析：**

1. **追查串口数据流：** `LOGI()` → PrintQueue → PrintTask → `HAL_UART_Transmit_DMA`。DMA 在后台不停发送 LOG，导致 HAL 的 `gState` 几乎永远为 `HAL_UART_STATE_BUSY_TX`
2. **追查 send_packet 路径：** 最初用 `HAL_UART_Transmit_DMA`，发现与 PrintTask 抢 DMA 通道。改为 `HAL_UART_Transmit`（阻塞），但 HAL 内部检查 `gState == HAL_UART_STATE_READY`——而 PrintTask 的 DMA 让 gState 永远是 BUSY → **函数直接返回 HAL_BUSY 静默丢弃数据**

**修复（走系统消息队列）：** 不使用 HAL 的任何 TX 函数，直接将帧数据打包成与 `LOGI` 一样的格式——`LogMsg_t{text[128], len}`，通过现有的 **PrintQueue** 排队。

```c
// send_packet 最后的发送改为：
memcpy(msg.text, s_tx_buf, idx);
msg.len = idx;
QueueSend(PrintQueue, &msg);  // 和 LOGI 走同一个队列！
```

同时改造 `PrintTask` 用 `rxMsg.len` 代替 `strlen(rxMsg.text)`，支持二进制帧传输。

**架构收益：** 所有输出（日志 + FOTA 协议帧）现在都通过同一个 PrintQueue → PrintTask → DMA 通道，顺序发送，无竞争、无丢帧。

---

### Bug #7: UART RX 中断未启动

**现象：** Bug #6 修复后 PKT_READY 能发出了，但是 PC 发送 PKT_INFO 后 MCU 永远不回复 ACK。上位机等了 3 秒超时。

**排查：**

1. **PKT_READY 能发出 → UART TX ✅**
2. **PC 超时收不到 PKT_ACK → MCU 没收到 PC 的数据**
3. **检查 RX 链：** `stm32f1xx_it.c` 的 `USART2_IRQHandler` → `HAL_UART_IRQHandler` → `HAL_UART_RxCpltCallback` → RingBuffer，逻辑链完整
4. **关键遗漏：** `HAL_UART_Receive_IT(&huart2, &g_rx_byte, 1)` **从未被调用过**。HAL 的 UART RX 状态机在**没有被第一次触发**之前，永远不会进入接收状态——即使 NVIC 的 USART2 中断已使能

**修复：** 在 FOTA 任务擦除完成后、进入接收循环前，调用一次：

```c
HAL_UART_Receive_IT(&huart2, &g_rx_byte, 1);  // 启动 RX 中断链
```

---

### Bug #8: Bootloader 的 0xFFFFFFFF 断点逻辑导致固件不完整

**现象：** 升级成功后 MCU 复位，**直接进 HardFault**。升级过程中 CRC32 校验、PKT_OK 全部正常。

**排查过程：**

1. **确认 Staging 数据正确：** 升级协议中每帧有 CRC16，全文件有 CRC32，MCU 全部校验通过才写标志位——硬件层面数据是完整的
2. **定位 Bootloader 拷贝代码：**

```c
// My_bootloader main.c 第 91 行 ← 就是这个！
word = *(__IO uint32_t*)src;
if (word == 0xFFFFFFFF) break;  // "数据到此为止"
```

3. **分析：** `.bin` 文件里天然存在 `0xFFFFFFFF` 填充——BSS 段、对齐间隙，编译器自动填入。Bootloader 读到第一个 `0xFFFFFFFF` 就认为"数据结束"停止拷贝，后面几 KB 实际的代码/数据没拷过去
4. **为什么之前不触发：** 原始 Bootloader（2.43KB，无 Flash HAL 驱动）的 `.bin` 文件较小，填充位置不同。加入 `stm32f1xx_hal_flash.c` 后固件变大了，填充 0xFFFFFFFF 恰好落在一个靠前的位置

**同时修复 FOTA 写入端：** `write_flash_buffer` 也有 `if (word != 0xFFFFFFFF)` 的跳过逻辑——导致 Staging 区中留下未写入的"洞"，读出来是 0xFF，Bootloader 同样会在这些洞处停止。

**修复：**

```c
// Bootloader: 去掉 if (word == 0xFFFFFFFF) break;
// 改为逐字拷贝全部 28KB:
for (uint32_t offset = 0; offset < STAGING_SIZE; offset += 4) {
    word = *(__IO uint32_t*)(STAGING_ADDR + offset);
    HAL_FLASH_Program(FLASH_TYPEPROGRAM_WORD, APP_START_ADDRESS + offset, word);
}

// flash_update.c: 去掉 if (word != 0xFFFFFFFF) 跳过逻辑
// 改为直接写:
HAL_FLASH_Program(FLASH_TYPEPROGRAM_WORD, dst_addr + i, word);
```

**教训：** 0xFFFFFFFF 不是"空"——在 Flash 中它是"已擦除状态"，但在固件二进制中是**合法的数据值**。任何以 0xFFFFFFFF 作为"结束标记"的逻辑在固件升级场景下都是 bug。

---

### Bug #9: 上位机与 RTOS 的适配性教训

**不是代码 bug，而是设计上的教训：**

| 你的 RTOS 特性 | 影响 | 在本项目的应对 |
|---------------|------|---------------|
| `SemaphoreGive` 在 ISR 中调用 `__enable_irq()` | ISR 中不能调 RTOS 函数 | Ring Buffer 无锁设计，ISR 只写 head，任务只读 tail |
| `TaskDelete(NULL)` 不符预期 | 不能删除自身 | FOTA 任务完成直接 `NVIC_SystemReset()` |
| 无软件定时器 | 协议超时靠自己 | `taskdelay` 累加计数器实现软超时 |
| 消息队列仅 10 槽 | 频繁 LOGI 会满 | FOTA 每 4KB 输出一次，不刷屏 |

---

### Bug #10: FOTA 传输期间中断完全锁死——按键无响应

**现象：** PC 向 MCU 传固件时，按按钮完全无反应。传输结束后按键操作一次性爆发执行（缓冲区积压的信号量一次性释放）。

**排查过程：**

1. **关键观察：** 传输期间串口终端持续收到 MCU 的 LOG 输出，说明 PrintTask(优先级 3)在运行。但 LedTask(优先级 2)的按键处理完全不触发
2. **分析接收循环：** `while(receiving)` 中 `RingBuffer_Read` 一直有数据（PC 持续发包）→ 代码永远走不到 `else { taskdelay(5); }` → 高优先级任务无法抢占 FOTA 任务？
3. **推翻猜测：** 按 RTOS 调度规则，LedTask(优先级 2) > FOTA(优先级 1)，LedTask 应该抢占 FOTA。能抢占却没响应 → 说明问题不在调度，而在**中断被物理屏蔽**
4. **定位 `write_flash_buffer`：** 写入 2KB = 512 个 word，每 word 写 ~45µs。`HAL_FLASH_Program` 内部调用 `__disable_irq`/`__enable_irq`。512 次累积锁死中断 **~23ms**，EXTI 按键中断被挡在外面
5. **综合效应：** 传输期间（~3 秒），每收到 8 包就触发一次 `write_flash_buffer`，每次锁中断 ~23ms。用户按一次键的 200ms 内，可能恰好落在锁中断窗口中 → 感知为"按键坏了"

**修复（两处加入让步点）：**

```c
// 修复 1: write_flash_buffer 每写 128 个 word 刷新看门狗 + 让出 CPU
void write_flash_buffer(...) {
    for (...) {
        HAL_FLASH_Program(...);
        if (++word_count >= 128) {
            word_count = 0;
            HAL_IWDG_Refresh(&hiwdg);
            taskdelay(3);   // ← 给 EXTI 中断一个执行窗口
        }
    }
}

// 修复 2: 接收循环主动让步计数
while (receiving) {
    if (RingBuffer_Read(&g_rx_ring, &byte)) {
        yield_counter++;
        FrameParser_Feed(byte, &frame);
        ...
        if (yield_counter >= 80) {   // ← 每 80 字节让一次
            yield_counter = 0;
            taskdelay(3);
        }
    }
}
```

**教训：** #1 `HAL_FLASH_Program` 的 `__disable_irq` 不是"一条指令"——连续调 512 次累积成宏观可见的中断锁死。**#2** 接收循环不能隐含依赖 `taskdelay` 来解决 CPU 占用——必须显式放入让步点。


### Bug #11: 内存分配器升级：first-fit → heap4 best-fit

**变更：** 将 `my_os_malloc`/`my_os_free` 从 first-fit + 单向链表 升级为 **best-fit + 双向链表 + 最小碎片约束** (heap4 风格)。

**动机：** 原算法虽能正常工作，但随着系统功能增多（FOTA、更多 IPC），碎片风险逐步积累。趁系统成熟前做算法升级，比事后修复更划算。

**算法选型分析：**

| 方案 | 原理 | 单片机可行性 | 结论 |
|------|------|------------|------|
| **位图内存池** | 固定块 + 位图标记，O(1) | ❌ 没有 MMU 就无法实现不同大小请求的虚拟地址连续映射。STM32F103 无 MMU | 不适用 |
| **伙伴算法** | 2ⁿ 幂分割，O(log N) | ⚠️ 理论上可行。但 12KB 堆上 2 的幂分割浪费严重（如请求 1300B → 分配 2048B），元数据开销也不小 | 性价比低 |
| **heap4** | best-fit + 双向链表 + 最小块约束 + 前后合并 | ✅ 开销仅 8 字节/块，碎片控制最优。实现 ~120 行 | **采用** |

**改动范围：** 仅 `rtos.c:33-144`，API 签名不变，其余文件零改动。

```c
// v0.5: first-fit + 单向链表
typedef struct MemBlock {
    struct MemBlock *next;   /* 单向 */
    uint32_t size;
} MemBlock_t;

// v0.6: best-fit + 双向链表
#define HEAP_MIN_BLOCK_SIZE  (sizeof(struct MemBlock) + 8)

typedef struct MemBlock {
    struct MemBlock *next;
    struct MemBlock *prev;   /* 新增加速移除 */
    uint32_t size;
} MemBlock_t;

// 分配：遍历所有块，选最小的满足块 (best-fit)
while (curr != NULL) {
    if (curr->size >= total_size && curr->size < best_size) {
        best = curr; best_size = curr->size;
    }
    curr = curr->next;
}

// 释放：双向链表插入，前后合并
block->next = curr;
block->prev = prev;
if (prev) prev->next = block; else freeListHead = block;
if (curr) curr->prev = block;
```

**三个关键收益：**

1. **Best-fit** —— 优先用尺寸刚好够的块，避免大块被切割成碎片。相比 first-fit（找第一个够用的），碎片增长速度更慢
2. **`HEAP_MIN_BLOCK_SIZE`** —— 分割时如果剩余部分小于此值，就不切、直接分配整块。防止产生无法被二次利用的"针尖"碎片。这是原 first-fit 实现中没有的
3. **双向链表** —— malloc 中摘除 best 块无需额外变量追踪 `prev`，代码更简洁。free 中的插入操作也更对称

**验证：** 编译 0 error 0 warning。RTOS 启动后 7 个任务创建成功。FOTA 全流程正常。

---
---

## 五、项目完整状态

- [x] Bootloader 正常启动，检测标志位并搬运固件
- [x] RTOS 含 6 业务任务 + 1 FOTA 任务
- [x] FOTA 任务上电自动擦除 Staging 并循环等待固件
- [x] PKT_READY 每 3 秒重发，PC 随时连接都能收到
- [x] PC 上位机完整功能：终端显示 + 固件传输 + 超时重传
- [x] 升级完成后自动重启，Bootloader 搬运 → 跳转新 App
- [x] 按键控制呼吸灯 + PB5，全功能正常
- [x] 编译开关 `FOTA_DEMO_NEW` 支持生成"测试版"固件用于升级验证
- [x] 硬件 IWDG 在擦除和写入期间稳定刷新，无超时复位

---

## 六、构建与使用

### 构建步骤

1. **Bootloader:** 打开 `My_bootloader/MDK-ARM/My_bootloader.uvprojx` → Rebuild All → Flash Download
2. **App:** 打开 `MDK-ARM/cubetest.uvprojx` → Rebuild All → Flash Download
3. **自动生成 .bin:** 编译完成后查看 `MDK-ARM/cubetest/cubetest.bin`

### 串口接线

```
STM32 PA2 (USART2_TX) ←→ USB-TTL RX
STM32 PA3 (USART2_RX) ←→ USB-TTL TX
GND ←→ GND
波特率: 115200
```

### FOTA 联调步骤

1. 先烧 Bootloader → 再烧 App
2. 运行 `python PC_Tool/fw_updater.py`
3. 打开串口 → 选择 `cubetest.bin` → 开始升级
4. 观察终端日志：`PKT_READY → PKT_INFO → ACK → PKT_DATA... → PKT_OK`
5. MCU 自动重启 → 呼吸灯恢复正常 → 升级成功

### 生成测试固件

在 Keil Options → C/C++ → Define 中追加 `,FOTA_DEMO_NEW`，Rebuild 后生成的 `.bin` 中，按键行为变为"仅翻转 PB5，不干扰呼吸灯"——便于直观验证升级是否生效。
