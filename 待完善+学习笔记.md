# 待完善

1.更新程序中，应该为flash解锁函数（HAL_FLASH_Unlock）加上互斥锁，虽然硬件自带了标志位不会同时写入，但是可能会导致意外的任务死等一直占用CPU（比如同/低优先级任务抢占之后发生死锁）

2.完善第一条之后我的项目就存在死锁风险，应该规避，可能的方案：1.给互斥锁添加优先级。2.给互斥锁增加超时放弃机制。3.持有锁时禁止任务调用阻塞api
3.内存管理：1.将任务栈大小参数暴露到任务创建函数里面。2.在任务栈底部添加魔数标记防止任务内操作意外栈溢出。3.在操作系统启动时遍历内存块获取所有空内存作为任务堆，尽可能最大化堆。
4.任务创建函数TaskCreate增加更多注释解释伪造寄存器逻辑，最好有图片说明。

5.我的代码有没有优先级继承来解决反转的逻辑？

# 学习笔记

## 1.内存分配

### 1.位图内存池

现代os主流方案，但是依赖于虚拟存储控制器实现访问连续性，单片机不可行。

### 2.伙伴算法

单片机上性能优秀，但是缺点是对于stm32的20kbram性价比太低，本身占用太大。

### 3.heap4 内存分配算法笔记
内存模型：整块连续堆内存，切割为动态可变大小内存块，无固定分页，和位图固定页模型完全不同。
块头结构：每一块内存（无论空闲 / 已分配）头部存储块总大小；仅空闲块额外附带双向指针：指向前、后两个链表内的空闲块，构成按物理地址升序的双向空闲链表。
空闲块查找：无随机寻址能力，只能顺序遍历双向空闲链表，找到第一个能满足申请大小的空闲块（首次适配）。
**分配流程**
① 遍历链表匹配满足尺寸的空闲块；
② 若取出块扣除需求后仍能容纳最小块头 + 对齐空间，则拆分剩余内存，生成新空闲块，替换原块在链表中的双向指针；
③ 将用于分配的块标记为已分配，移出空闲链表，返回可用内存地址。
**回收 / 合并流程**
① 将待释放块标记为空闲，按地址升序插入双向空闲链表，更新前后空闲块的链表指针；
② 通过内存地址计算，判断当前块物理前、后位置是否存在空闲块（不依赖链表 prev/next 指针）；
③ 若物理相邻存在空闲块，将相邻块从链表摘除，两块尺寸合并，保留一块在链表中，消除外部碎片。