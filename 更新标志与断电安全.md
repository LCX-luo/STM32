# Flash 更新标志位与断电安全设计

## Flash 硬件特性

STM32F103 的 Flash 控制器有两个基本操作：

| 操作 | 位变化方向 | 最小操作单位 | 耗时 | 说明 |
|------|-----------|------------|------|------|
| **Program** | **1 → 0** | 1 word (4 字节) | ~45μs | 将指定位从 1 变为 0 |
| **Erase** | **0 → 1 → 全 1** | **1 页 (1KB)** | ~30ms | 将该页所有位强制恢复为 1 |

**核心约束：** Program 不能把 0 写回 1。要把 0 写回 1，必须 Erase——而 Erase 会擦除**整页**（1KB），不单擦一个字。

```
0xFF → Program(0xA5) → 0xA5  (1→0，正确)
0xA5 → Program(0x00) → 0x00  (1→0，正确)
0x00 → Program(0xFF) → 0x00  (❌ 0 不能写回 1)
0x00 → Erase → 0xFF          (✅ 整页恢复)
```

---

## 当前标志位页逻辑

### Flash 布局

```
0x08001C00 ┌──────────────────────┐
           │  第 7 页（1KB）       │
           │  用途：更新标志位     │
0x08001FFC │  [MAGIC: 4 字节]     │ ← 更新标志位
0x08001FFF └──────────────────────┘
```

### 更新流程

```
App 下载完成 → set_update_flag()：
  1. Erase 第 7 页        ← 全页变 0xFF
  2. Program 0xA5A5A5A5 到 0x08001FFC  ← 4 字节写为 0xA5A5A5A5
  3. Reset

Bootloader 启动 → 读 0x08001FFC：
  = 0xA5A5A5A5 → 执行 copy_new_firmware()
    1. Erase App 区
    2. 逐字拷贝 Staging → App
    3. Erase Staging 区
    4. Erase 第 7 页（MAGIC 回到 0xFFFFFFFF）
    5. 跳转 App
  = 0xFFFFFFFF → 直接跳 App（无更新）
```

### 断电风险分析

| 阶段 | 操作 | 断电后果 | 恢复能力 |
|------|------|---------|---------|
| ① App 收固件 | 写 Staging | Staging 数据不完整 | ✅ 下次启动 CRC 校验不通过 → 不写 MAGIC → 复位后走旧 App |
| ② 写 MAGIC | Program 0xA5 | 0xFF → 部分写入（如 0x00FFA5A5） | ✅ Bootloader 读到的不是 0xA5A5A5A5 → 当无更新处理 |
| ③ 擦 App 区 | Erase | App 被擦，Staging 完整 | ✅ MAGIC 还在 → Bootloader 重新执行全流程 |
| ④ 拷 Staging→App | Program | App 写了一半 | ⚠️ MAGIC 还在，但需要知道已拷到哪一页 |
| ⑤ 擦 Staging+标志 | Erase | 不重要，App 已完整 | ✅ |

**阶段 ④ 是目前唯一缺少进度保护的部分。** 如果 28 页拷到第 15 页时断电，重启后 Bootloader 又从第 0 页开始拷——效率损失但没有安全性问题。但如果总页数多、拷贝时间长，效率损失不可忽略。

---

## 改进方案：进度位图

### 设计

利用 Flash **1→0 不需要擦除**的特性，在标志位页中分配 64bit 作为进度位图：

```
0x08001C00 ┌──────────────────────┐
           │  进度位图 (8 字节)    │ ← Bit N = 1 表示第 N 页未拷贝
           │                      │     Bit N = 0 表示第 N 页已拷贝
           │                      │
           │  保留                  │
0x08001FFC │  [MAGIC: 4 字节]     │ ← 更新标志位
0x08001FFF └──────────────────────┘
```

### 拷贝流程

```c
// 初始化时：所有 bit = 1 (全 0xFF，擦除后的状态)
// progress = 0xFFFFFFFFFFFFFFFF

void copy_new_firmware(void)
{
    uint64_t progress = *(__IO uint64_t*)0x08001C00;

    // 逐页拷贝，跳过已完成的页
    for (int page = 0; page < 28; page++) {
        if (progress & (1ULL << page)) {
            // 该页未拷贝 → 执行拷贝
            copy_one_page(page);
            // 标记完成（1→0，只需要 Program，不需要 Erase）
            uint64_t mask = ~(1ULL << page);
            HAL_FLASH_Program(FLASH_TYPEPROGRAM_WORD, 0x08001C00,
                *(__IO uint32_t*)0x08001C00 & ((uint32_t*)&mask)[0]);
        }
    }

    // 拷贝完成：擦除第 7 页（MAGIC + 进度位图一起清 0）
    erase_flag_page();  // 整页回到 0xFF → MAGIC=0xFFFFFFFF → 下次不走拷贝
}
```

### 断电恢复流程

```
断电 → 重启
  → 读 MAGIC = 0xA5A5A5A5
  → 读 progress ≠ 0xFFFFFFFFFFFFFFFF
    → 上次拷贝未完成，跳过已完成的页继续
  → 拷贝全部完成 → 擦除第 7 页
  → 跳 App
```

### 改进后断电风险

| 阶段 | 操作 | 断电后果 |
|------|------|---------|
| ④-① | Program 进度 bit 前 | 该页已拷到 Flash 但进度没记录 → 下次重拷该页（安全但低效）|
| ④-② | Program 进度 bit 后 | 该页拷贝成功已记录 → 继续下一页 |

**没有任何阶段会变砖。** 最坏情况是重复拷一页（~30ms），这是可以接受的代价。

---

## 其他设计方向

### 方向一：双区 OTA（A/B Swap）

```
当前：Bootloader(7K) | Flag(1K) | App(28K) | Staging(28K)
改为：Bootloader(7K) | Flag(1K) | App_A(28K) | App_B(28K)
- 固件直接收进 App_B
- Bootloader 检查标志位，切换启动到 App_B
- 不需要拷贝，零拷贝风险
```

**缺点：** App 从 28KB 缩减为...其实总量不变，28K+28K 刚好。但 Bootloader 不再需要拷贝，只需要改 VTOR 跳转即可。

**断电安全等级：** 最高。固件接收阶段断电 → Staging 不完整 → 下次重收。标志切换阶段断电 → 下次还是从旧区启动。没有变砖窗口。

### 方向二：外部 Flash

```
W25Q64 (8MB) 等 SPI Flash：
- 价格 < 2 元
- 64MBit = 8MB
- 可以存 30+ 个固件版本
- 断电完全不影响主 Flash
```

但需要额外的 SPI 引脚和驱动代码。

### 方向三：保留现状（不做进度位图）

对于 28 页（~840ms 拷贝时间），即使每次断电从头拷，也只需要多花 840ms。以你的使用场景（个人项目、非工业现场），这个开销可以接受。

---

## 总结

| 方案 | 断电安全 | 实现量 | 适用场景 |
|------|---------|--------|---------|
| **当前（无进度位图）** | ✅ 不会变砖，但断电后重头拷 | 0 行 | 个人项目 |
| **+进度位图（推荐）** | ✅ 断电后从中断页继续 | ~20 行 | 需要升级效率时 |
| **A/B Swap** | ✅ 最高 | ~100 行 | 对安全零容忍 |
| **外部 Flash** | ✅ 最高 | ~200 行 + 硬件 | 量产产品 |
